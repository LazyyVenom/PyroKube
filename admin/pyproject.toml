[project]
name = "pyrokube-backend"
version = "0.1.0"
description = "Backend folder for admin provisioning ok..."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.140.0",
    "jinja2>=3.1.6",
    "pydantic-settings>=2.14.2",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.51",
]
